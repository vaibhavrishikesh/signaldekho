.gradle/
build/
local.properties
.idea/
*.iml
.DS_Store
/captures

# Signing material — never commit. Losing these means losing the Play listing,
# leaking them means someone else can ship updates as us.
*.jks
*.keystore
keystore.properties
